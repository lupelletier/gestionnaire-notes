/*import { test, expect } from '@playwright/test';

test('test', async ({ page }) => {
    await page.goto('https://demo.playwright.dev/todomvc/');
    await page.goto('https://demo.playwright.dev/todomvc/#/');
    await page.goto('http://localhost:5174/');
    await page.getByRole('button', { name: 'Add Mark' }).click();
    await page.getByLabel('Title:').click();
    await page.getByLabel('Title:').fill('This is a title');
    await page.getByLabel('Title:').press('Tab');
    await page.getByLabel('Comment:').fill('This is a comment about a very bad mark');
    await page.getByLabel('Mark (out of 20):').click();
    await page.getByLabel('Mark (out of 20):').fill('2');
    await page.locator('form').getByRole('button', { name: 'Add Mark' }).click();
    await page.getByRole('button', { name: 'View' }).click();
    await page.getByText('This is a titleThis is a comment about a very bad markMark: 2/').click();
});*/